
data:
  path: "data/raw/WA_Fn-UseC_-Telco-Customer-Churn.csv"
  
split:
  test_size: 0.2
  random_state: 42

model:
  name: "RandomForest" # Opciones: "RandomForest", "LogisticRegression"
  n_estimators: 100
  max_depth: 10
  C: 1.0 # Hiperparámetro exclusivo para LogisticRegression

paths:
  model_save: "models/model.pkl"