data:
  raw_path: "data/raw/WA_Fn-UseC_-Telco-Customer-Churn.csv"
  test_size: 0.2
  random_state: 42

model:
  name: "RandomForest"
  # Aquí agregamos la sección de hiperparámetros que piden las instrucciones
  params:
    n_estimators: 100
    max_depth: 10
    random_state: 42

paths:
  model_save: "models/model.pkl"